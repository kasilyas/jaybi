# --- Jaybi Frontend (Vite) ---
# Copier ce fichier en .env.local et ajuster les valeurs.

# Bypass 2FA / auto-login de test en développement.
# NE JAMAIS mettre à true en production.
VITE_DEV_BYPASS=true

# Clé API Gemini (côté client uniquement pour le MVP frontend).
# @security En v0.2, les appels Gemini passeront par le backend.
VITE_GEMINI_API_KEY=
